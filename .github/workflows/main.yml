name: E2E tests

on:
      push:
        branches:
          - master
      pull_request:
        types: [opened, synchronize, reopened]
        branches:
          - master

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Run E2E test
        uses: cypress-io/github-action@v1
        with:
          config: baseUrl=https://barrigareact.wcaquino.me/